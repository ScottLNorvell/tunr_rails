<h1>Songs</h1>

<ul class='songs'>
	<% @songs.each do |song| %>
		<li>
			<h1><%= song.title %></h1>
			<p>
				<%= song.artist.name %>
			</p>
			<p>
				<%= song.album.title %>
			</p>
		</li>
	<% end %>

</ul>